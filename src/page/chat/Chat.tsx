import React from 'react';
import Header from '../../components/header/Header';
type chatProps = {
  
};

const Chat: React.FC<chatProps> = ({  }) => {
  

  return (
    <div>
      <Header/>
      
    </div>
  );
}

export default Chat;